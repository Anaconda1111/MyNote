//#include "LeftistHeap.h"
#include "stdio.h"
int main() {
    int n,m;
    int A[100],B[100];
    while (scanf("%d %d",&m,&m)!=EOF){
        if (n==0&&m==0)
            break;
        for (int i = 0; i < n; ++i) {
            scanf("%d",A+i);
        }
        for (int i = 0; i < m; ++i) {
            scanf("%d",B+i);
        }

    }
    return 0;
}
